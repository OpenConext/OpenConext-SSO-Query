# OC SSO-Query - Release

This module is used to create a release of OC SSO-Query and contains documents like installation instructions and 
release notes.

The release notes are written in Markdown files and are transformed to html files while generating a Maven site.
